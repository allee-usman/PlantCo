import { View, Text } from 'react-native';
import React from 'react';

const CustomText = () => {
	return (
		<View>
			<Text className="text-2xl font-semibold text-gray-800">
				Hello from CustomText
			</Text>
		</View>
	);
};

export default CustomText;
